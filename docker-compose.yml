services:
  sandbox:
    image: ghcr.io/agent-infra/sandbox:latest
    container_name: rasputin-sandbox
    ports:
      - "8080:8080"
      - "18200:8200"
      - "18888:8888"
    volumes:
      - ./outputs:/workspace/outputs
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/v1/health"]
      interval: 10s
      timeout: 3s
      retries: 5
    profiles: ["cpu", "gpu-single", "gpu-multi"]
    restart: unless-stopped

  searxng:
    image: searxng/searxng:latest
    container_name: rasputin-searxng
    ports:
      - "8889:8080"
    volumes:
      - ./searxng-config:/etc/searxng:rw
    environment:
      SEARXNG_BASE_URL: http://localhost:8889/
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 3s
      retries: 5
    profiles: ["cpu", "gpu-single", "gpu-multi"]

  comfyui:
    image: ghcr.io/comfyanonymous/comfyui:latest
    container_name: rasputin-comfyui
    ports:
      - "8188:8188"
    volumes:
      - ${COMFYUI_MODELS_DIR:-~/comfyui-models}:/workspace/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8188/"]
      interval: 30s
      timeout: 5s
      retries: 5
    profiles: ["gpu-single", "gpu-multi"]
