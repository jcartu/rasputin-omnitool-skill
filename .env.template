# Langfuse self-hosted secrets — change before exposing publicly
LANGFUSE_NEXTAUTH_SECRET=changeme-dev-secret
LANGFUSE_SALT=changeme-dev-salt

# GPU model directories (host paths mounted into containers)
COMFYUI_MODELS_DIR=~/comfyui-models
WAN_MODELS_DIR=~/wan-models

# Optional: override images if you build local variants
# WAN_IMAGE=ghcr.io/wan-video/wan-2.1-server:latest
# MUSICGEN_IMAGE=ghcr.io/musicgen/musicgen-server:latest

# Required for agent loop (no defaults — must be set by user)
OPENCODE_ZEN_API_KEY=
ANTHROPIC_API_KEY=

# Cost ceiling per goal (USD)
RASPUTIN_OMNITOOL_MAX_COST_USD=0.50
